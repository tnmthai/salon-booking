{
    "name": "salon-booking",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && rm -rf dist && npm install && npm run build",
        "start": "cd server && node index.js"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "jsonwebtoken": "^9.0.3"
    }
}