{
    "name": "salon-booking",
    "version": "1.0.1",
    "private": true,
    "scripts": {
        "build": "cd server && npm install && cd ../client && npm install && npm run build",
        "start": "cd server && node index.js"
    },
    "dependencies": {
        "bcryptjs": "^3.0.3",
        "jsonwebtoken": "^9.0.3"
    }
}